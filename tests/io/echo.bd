import std::io.
import std::string.

rule main() -> i64:
  set line: string = std::io::read_line().
  set out: string = std::string::concat(line, "!").
  std::io::print(out).
  yield std::string::len(out).
end
