import std::thread.

rule worker() -> i64:
  yield 3.
end

rule main() -> i64:
  set t: Thread = std::thread::spawn("worker").
  set first: i64 = std::thread::join(t).
  set second: i64 = std::thread::join(t).
  yield first + second.
end
