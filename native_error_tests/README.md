# Native error fixtures

This folder contains BirdDisk programs expected to fail in the native backend.

Run them with:
```sh
./target/debug/birddiskc test --json --engine native --dir native_error_tests
```
