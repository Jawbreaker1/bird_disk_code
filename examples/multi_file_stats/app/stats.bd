import app::math.

book Stats:
  rule init(self: Stats) -> Stats:
    yield self.
  end

  rule compute(self: Stats) -> i64:
    set base: i64 = 21.
    set math: Math = new Math().
    set doubled: i64 = math::double(base).
    yield doubled + 1.
  end
end
