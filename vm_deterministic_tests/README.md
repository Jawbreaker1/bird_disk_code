# VM deterministic fixtures

These fixtures are intended to run with deterministic VM scheduling enabled.

Run them with:
```sh
./target/debug/birddiskc test --json --engine vm --dir vm_deterministic_tests --deterministic
```
